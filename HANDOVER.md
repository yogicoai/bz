# 인계 문서 — B2B·영문 메일 관리 대시보드

인수인계 시 이 문서만 따라가면 된다. 사용법은 앱 안의 `/guide` 화면에 있고,
설계 배경과 코드 규약은 `CLAUDE.md` 에 있다.

---

## 1. 넘기기 전에 반드시 할 것

아래 세 가지는 **하지 않으면 기능이 동작하지 않는다.**

| 할 일 | 어디서 | 안 하면 |
|---|---|---|
| 브리핑 수신 주소를 `david@yogico.kr` 로 변경 | `/settings` | 브리핑이 테스트 주소(`fe@yogico.kr`)로 계속 감 |
| `APP_BASE_URL` 을 실제 배포 주소로 변경 | `.env.local` | 브리핑 메일의 링크가 `localhost:5900` 이라 열리지 않음 |
| `APP_SECRET` · `CRON_SECRET` 을 랜덤 값으로 재생성 | `.env.local` | 세션 위조·크론 무단 호출 가능 |

그리고 **`APP_AUTH_DISABLED` 은 절대 설정하지 말 것.** 설정하면 비밀번호 없이 전부 열린다.

---

## 2. 현재 상태

```
메일          1,763통 수집 (거래처 폴더 11개 + INBOX)
로컬 분석      전량 완료 — 답변필요·기한·긴급도
AI 요약·번역   최근 30일 179/179 완료 · 전체 279통
API 키        미설정 (앱이 Claude 를 부른 적 없음 = 과금 0원)
```

**요약이 100% 완료된 거래처** — Osstem Pharma Vussen 70 · Schestowitz Israel 32 ·
Distribution Group Turkey 30 · Blue Marble Israel 18 · Trade Vendors 4

Schestowitz 32통은 전부 히브리어다. 한글 번역이 붙어 있어 시연에 쓰기 좋다.

---

## 3. 비용

돈이 나가는 곳은 **AI 요약**과 **답장 초안** 둘뿐이다. 수집·분류·광고필터·기한추출·브리핑은 전부 무료다.

| 모델 | 1통 | 하루 5통 · 월 22일 | 하루 20통 · 월 22일 |
|---|---|---|---|
| **Haiku 4.5** (현재 설정) | ₩9 | ₩990 | ₩3,961 |
| Sonnet 5 | ₩27 | ₩2,971 | ₩11,883 |
| Opus 5 | ₩45 | ₩4,951 | ₩19,804 |

실측 기준(인용부 제거 후 평균 1,611자)이다. 모델은 `/settings` 에서 언제든 바꿀 수 있다.

**비용이 튀지 않는 이유** — 하루 상한 20통, 광고·자동발송 제외,
자사 발신 메일 제외(전체의 3분의 1), 답장에 딸려온 인용부 제외(본문의 90%),
이미 요약한 것은 재요약 안 함.

---

## 4. API 키를 넣을 때

`.env.local` 에 `ANTHROPIC_API_KEY` 를 넣으면 그때부터 유료 기능이 살아난다.

**넣기 전에 확인할 것**

- `autoAnalyze` 가 꺼져 있는가 (`/settings`) — 켜면 수집할 때마다 상한 없이 요약이 붙는다
- `dailyAnalyzeLimit` 이 20인가 — 하루 비용 상한

**넣은 직후 할 것**

메일 하나를 열어 **AI 번역·요약 실행**을 눌러본다. 9원이면 확인 비용으로 충분하다.
이 경로는 키가 없어 실제 호출로 검증된 적이 없다. 코드와 SDK 타입 대조까지만 마친 상태다.

정상이면 다음 날 아침부터 그날 온 메일이 자동으로 요약된다.

---

## 5. 과거 메일을 채우려면

현재 1,145통 중 279통이 요약돼 있다. 나머지를 채우는 방법은 두 가지다.

**A. 앱에서 (권장)** — 필요한 메일을 열어 버튼을 누른다. 금액이 먼저 표시된다.

**B. 한 번에** — 큰 덩어리는 Dangaard 296통·Yogibo Japan 335통이다.
둘 다 채워도 631통 × 9원 ≈ **5,700원**이다.

1년 전 뉴스레터까지 번역해 둘 실익은 크지 않다. 진행 중인 거래처만 채우는 편이 낫다.

---

## 6. 자동으로 도는 것

**매일 오전 8시 (KST)** — `vercel.json` 의 `0 23 * * *` (UTC 23:00, 전날)

브리핑이 담는 범위는 **직전 24시간**이다(어제 9시 ~ 오늘 9시).
달력 하루로 잡으면 09시에 도는 크론이 그날 00~09시만 보게 되는데,
실측하면 메일의 89%가 09시 이후에 온다 — 유럽·이스라엘 업무시간이 한국 오후다.
주말을 빼지 않는 이유도 같다. 이스라엘은 일요일이 평일이라 그날 메일이 21통 있다.
새 제안이 없는 날은 어차피 발송을 건너뛰므로 매일 돌려도 주말에 메일이 가지 않는다.

```
1. 수집        12개 폴더에서 새 메일만              무료
2. 거래처 분류  폴더 → 발신자 → 도메인 → 제목        무료
3. 광고 필터    광고·자동발송·읽음확인·부재중응답 제외  무료
4. 로컬 분석    답변필요·기한·긴급도                 무료
5. AI 요약     최신순 20통 (키 없으면 건너뜀)        유료
6. 브리핑      급한 것 5건 + 나머지 건수 → 메일 발송  무료
```

수집이 실패해도 요약·브리핑은 계속된다. 새 제안이 없는 날은 메일을 보내지 않는다.

---

## 7. 배포

Vercel 기준이다.

```
필수 환경변수
  EMAILDATA_URI        Mongo (DB명 emaildata)
  APP_SECRET           랜덤 재생성
  CRON_SECRET          랜덤 재생성
  APP_BASE_URL         실제 배포 주소  ← 브리핑 링크에 쓰임
  IMAP_PASS / SMTP_PASS  이카운트 웹메일 비밀번호
  ANTHROPIC_API_KEY    유료 기능을 쓸 때만
  CLAUDE_MODEL         기본 claude-opus-5 (설정 화면이 우선)

설정하면 안 되는 것
  APP_AUTH_DISABLED    설정하면 인증이 통째로 꺼진다
  DOC_WRITE_LOCAL      서버리스 디스크는 휘발성이라 무의미
```

`MAIL_DRY_RUN` 은 현재 `0`(실발송)이다. 테스트 중에는 `1`로 두면 콘솔에만 기록된다.

---

## 8. 비밀번호 — 두 종류가 따로 있다

대표가 알려준 **웹메일 계정 비밀번호**와, 이 앱에 들어갈 때 쓰는 **접근 비밀번호**는 별개다.
둘 다 앱 안에서 바꿀 수 있고, 코드나 서버를 건드릴 필요가 없다.

### 앱 접근 비밀번호 — `/account`

이 앱에 로그인할 때 쓰는 비밀번호다. 메일 계정과 무관하다.

- 현재 비밀번호를 입력해야 바꿀 수 있다 (틀리면 거부 — 확인함)
- DB 에 scrypt 해시로만 저장된다. 평문은 어디에도 남지 않는다
- 바꿔도 **다른 기기의 로그인은 유지**된다

### 웹메일 계정 비밀번호 — `/settings`

이카운트 웹메일(`david@yogico.kr`)의 실제 비밀번호다. IMAP 수신과 SMTP 발송에 쓴다.

- 화면에는 **`저장됨` 표시만** 나오고 비밀번호 자체는 절대 내려오지 않는다 (확인함)
- **빈칸으로 두면 기존 값이 유지**된다. 다른 설정만 바꾸다가 실수로 지워지지 않는다 (확인함)
- 바꿀 때만 새 값을 입력하면 된다

**대표가 웹메일 비밀번호를 바꾸면** 수집과 발송이 멈춘다.
`/settings` 에서 새 비밀번호를 넣고 **연결 테스트**를 눌러 폴더 목록이 나오면 복구된다.
서버 재배포는 필요 없다.

> 대표가 계정을 계속 공유하는 것이 부담스럽다면, 이카운트에서
> **이 앱 전용 계정을 하나 더 만들어** 거래처 폴더 읽기 권한만 주는 방법도 있다.
> 그러면 대표가 본인 비밀번호를 바꿔도 앱은 영향을 받지 않는다.

---

## 9. 알아둘 제약

- **깨진 메일 1통** — 4/21 오스템파마 '유럽 RP 서류'는 발신 측 MIME 손상으로 본문 36%가 복구 불가다.
  추측으로 메우지 않고 그렇게 표시해 두었다. 첨부는 정상이라 내려받을 수 있다.
- **AI 경로 미검증** — 키가 없어 실제 호출을 해본 적이 없다. 첫 1통 테스트를 권한다.
- **거래처 폴더가 곧 학습 데이터** — 대표가 웹메일에서 폴더를 새로 만들면
  `/settings` 의 수집 폴더 목록에 추가해야 그 폴더도 수집된다.
- **읽음/안읽음 없음** — 사이드바 숫자는 '아직 손대지 않은 것'이며,
  브리핑에서 체크하거나 상태를 바꿔야 줄어든다.

---

## 10. 문제가 생기면

| 증상 | 확인할 것 |
|---|---|
| 새 메일이 안 들어옴 | `/settings` → 연결 테스트. 수집 폴더 목록이 비어 있지 않은지 |
| 브리핑 메일이 안 옴 | 수신 주소 설정 여부, `MAIL_DRY_RUN`, SMTP 비밀번호 |
| 브리핑 링크가 안 열림 | `APP_BASE_URL` 이 실제 주소인지 |
| 요약 버튼이 오류 | `ANTHROPIC_API_KEY` 설정 여부 |
| 비용이 예상보다 큼 | `/settings` 의 `autoAnalyze` 가 켜졌는지, 모델이 Opus 인지 |
| 광고가 자꾸 통과함 | `/settings` 차단 도메인·제목 키워드에 추가 (코드 수정 불필요) |
| 갑자기 수집·발송이 멈춤 | 대표가 웹메일 비밀번호를 바꿨는지 확인 → `/settings` 에서 갱신 후 연결 테스트 |
| 앱 비밀번호를 잊음 | `.env.local` 의 `APP_PASSWORD` 를 새로 설정하면 최초 로그인 시 DB 에 반영된다 |

수집 스크립트를 직접 돌릴 때는 **실패 건수를 반드시 확인할 것.**
조회·신규 건수만 보면 전량 실패해도 정상처럼 읽힌다(실제로 그렇게 가려진 적이 있다).

```bash
npm run learn-folders -- 600   # 거래처 폴더 깊게 수집
npm run ingest                 # 1회 수집
node scripts/reanalyze-local.js  # 로컬 재분석 (무과금)
```
